// The postfix and prefix forms
var a = 1, b = 1;
alert(++a); // prefix form return the new value
alert(b++); //post form returns the old value
alert(a); //2, increament once
alert(b); //2, increament once
